import type { ResolvedConfig } from "../plugin/index.js";
import type { BundleMetadata } from "./types.js";
export declare const DEFAULT_LIMIT = 150000;
export default function analyze({ bundlemeta, config, version, }: {
    bundlemeta: BundleMetadata;
    config: ResolvedConfig;
    version: string;
}): void;
